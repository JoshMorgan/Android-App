<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="info.tregmine.app"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:targetSdkVersion="20"
        android:minSdkVersion="8" />

    <application
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name" >
        <activity
            android:name="info.tregmine.app.LoginActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Menu Activity -->
        <activity android:name="info.tregmine.app.MenuActivity" >
        </activity>

        <!-- Chat Activity -->
        <activity android:name="info.tregmine.app.ChatActivity" >
        </activity>

        <!-- Forums Activity -->
        <activity android:name="info.tregmine.app.ForumsActivity" >
        </activity>

        <!-- Reports Activity -->
        <activity android:name="info.tregmine.app.ReportsActivity" >
        </activity>

        <!-- Donate Activity -->
        <activity android:name="info.tregmine.app.DonateActivity" >
        </activity>

        <!-- Commands Activity -->
        <activity android:name="info.tregmine.app.CommandsActivity" >
        </activity>

        <!-- Support Activity -->
        <activity android:name="info.tregmine.app.SupportActivity" >
        </activity>
    </application>

</manifest>